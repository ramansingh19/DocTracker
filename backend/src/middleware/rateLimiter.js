export function configureRateLimiter() {
  // Placeholder for express-rate-limit integration.
  // Keeps middleware chain intact while allowing easy upgrade later.
  return (_req, _res, next) => next();
}

